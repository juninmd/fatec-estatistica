@model dynamic

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@if (Model.EstratificadaProporcional.Any() == false)
{
    <div class="alert alert-warning alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
        <strong>Você ainda não preencheu os paramêtros! Por favor, preencha os paramêtros.</strong>
    </div>
}

<div class="fixed-action-btn" data-toggle="modal" data-target="#myModal" title="Entendendo a metodologia">
    <a href="javascript:void(0)" class="btn btn-warning btn-fab">
        <i class="material-icons">reorder</i>
        <div class="ripple-container"></div>
    </a>
</div>

<div class="well bs-component">
    <fieldset>
        <legend>Aleatória Simples</legend>
        <div class="form-group">


            <div class="col-xs-12">
                <blockquote class="small">
                    Uma amostra aleatória simples é usar um subconjunto de indivíduos (amostra) apartir de um conjunto maior (população) por um processo que garanta que
                    <ul>
                        <li>
                            Todos os indivíduos da população têm a mesma probabilidade de ser escolhidos para a amostra;
                        </li>
                    </ul>
                    <small class="pull-right">
                        Sorteamos uma quantidade de números X [AMOSTRA] entre um total de população de Y [POPULAÇÃO]
                    </small>

                </blockquote>
            </div>
        </div>
    </fieldset>

</div>

<div class="well bs-component">
    <form class="form-horizontal" method="post" action="@Url.Action("Index")">
        <fieldset>
            <legend>Parametrização</legend>
            @if (@Html.ViewData.ModelState.IsValid == false)
            {
                <div class="alert alert-warning alert-dismissable" style="background-color: #610883;">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                    <div class="row-action-primary">
                        <i class="material-icons" style="color: white;float: left;">error</i>
                    </div>
                    <strong>@Html.ValidationMessage("error")</strong>
                </div>
            }
            <div class="form-group">
                <label class="col-md-2 control-label">Quantidade da Amostra</label>

                <div class="col-md-4">
                    <input class="form-control" required placeholder="@Model.Amostra" name="amostra" type="number">
                </div>

                <label class="col-md-2 control-label">População</label>

                <div class="col-md-4">
                    <input class="form-control" required placeholder="@Model.Populacao" name="populacao" type="number">
                </div>

                <div id="divEstratoPai">
                    <label class="col-md-2 control-label">Estratos</label>

                    <div id="divEstrato" class="col-md-4">
                        <input class="form-control" required name="estrato" type="number">
                    </div>
                    <div class="col-md-3">
                        <input value="Adicionar Estrato" class="btn btn-info" type="button" onclick="funcaoAdicionarEstrato()">
                    </div>
                    <div class="col-md-3">
                        <input value="Remover Todos Estratos" class="btn btn-warning" type="button" onclick="funcaoRemoverTodosEstratos()">
                    </div>

                </div>

                <hr />


                <label class="col-md-2 control-label">Resultados</label>

                @foreach (var item in Model.EstratificadaProporcional)
                {
                    <text>
                        <div class="col-md-10">
                            <p> O @(item.IdEstrato + 1) Estrato deverá ter @(item.QtdEstrato) elemento(s) entrevistado(s).</p>
                        </div>

                    </text>
                }
            </div>

            <div class="col-md-10 col-md-offset-2">
                <button type="reset" class="btn btn-default">Limpar</button>
                <button type="submit" class="btn btn-primary">Enviar<div class="ripple-container"></div></button>
            </div>
        </fieldset>
    </form>
</div>